@model IEnumerable<WebApplication6.Models.Samsung>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Galaxy1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Galaxy2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Galaxy3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Galaxy1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Galaxy2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Galaxy3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.phones }) |
            @Html.ActionLink("Details", "Details", new { id=item.phones }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.phones })
        </td>
    </tr>
}

</table>
